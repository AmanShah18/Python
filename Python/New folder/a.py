# math={"aman":77,"shah":88}# dictionary is denoted by {} bracket
# print(math)
# math["sarvesh"]=80#add to the dictionary 

# for key, value in math.items(): #.items is a inbuilg funtion
#     print(key,"-->",value)

#-----------------------------------------------------------------------
    # dictionary keys and values
    # values
    # any type(immutable and mutable)
    # can be duplicates
    # diictionary values can be lists, and even dictionaries
    # keys
    # must be unique
    # immutable type(int, string, float, bol)
    # d={4:{1:0, 3:2}, ''const':{}}
    # sets
    # a set is an unordered collection of elements
    # sets are mutable, so we can easily add or remove items 
# #---------------------------------------------------------------------
# a={1,2,3}
# b={2,3,4}
# u={1,2,3,4,5,6,7,8,9}
# print(a.union(b).union(u))
# print(a.intersection(b))
# print(len(a))
# print(b.difference(a))
# print(a.symmetric_difference(b))# ANB-U
